apiVersion: extensions/v1beta1
kind: Ingress
metadata:
  name: single-service
  annotations:
    kubernetes.io/ingress.class: azure-application-gateway
spec:
  backend:
    serviceName: nginx
    servicePort: 80