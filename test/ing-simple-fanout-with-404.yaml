apiVersion: extensions/v1beta1
kind: Ingress
metadata:
  name: simple-fanout-with-404
  annotations:
    kubernetes.io/ingress.class: azure-application-gateway
spec:
  rules:
  - host: fanout404.test
    http:
      paths:
      - path: /foo/*
        backend:
          serviceName: nginx-foo
          servicePort: 80
      - path: /bar/*
        backend:
          serviceName: nginx-bar
          servicePort: 80
  backend:
    serviceName: nginx
    servicePort: 80